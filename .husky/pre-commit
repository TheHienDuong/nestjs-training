# Git hook pre-commit — runs BEFORE a commit is created.
#
# Only runs lint-staged: eslint + prettier on EXACTLY the staged files.
# Intentionally does NOT run the full test suite here — a slow hook is a hook
# that gets bypassed with --no-verify. Full tests are CI's job (see .github/workflows/ci.yml).
pnpm exec lint-staged
