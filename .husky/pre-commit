# Git hook pre-commit — runs BEFORE a commit is created.
#
# Only runs lint-staged: eslint + prettier on ONLY the files that are currently staged.
# Intentionally does NOT run the full test suite here — slow hooks get skipped with
# --no-verify. Full tests are handled by CI (see .github/workflows/ci.yml).
pnpm exec lint-staged