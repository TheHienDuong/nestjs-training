# L07 Closeout and L08 Start Implementation Plan

> **For Hermes:** Execute only after the approved mutation gates are confirmed in the active chat.

**Goal:** Complete the approved L07 operational closeout and resolve the authoritative L08 ticket before dispatching the next lesson wave.

**Architecture:** Keep VN `main` as the source of truth, tag the verified VN merge commit, then create a clean English-only GitLab mirror from the merged GitHub EN tree. Resolve L08 from Linear before deriving scope, branch, or agent ownership; do not infer implementation from the roadmap alone.

**Tech Stack:** Git/GitHub CLI, GitLab remote, Linear MCP through the authorized reader agent, NestJS 11, Prisma, pnpm, herdr worktrees/panes.

---

## Phase A — L07 tag

### Task A1: Verify VN merge and clean state

- Confirm PR #87/#88/#89 are merged and PR #90 is merged.
- Fetch `origin` refs and verify `origin/main` is clean and points to the VN closeout merge commit `3f834a4e`.
- Resolve any stale local remote-tracking ref without modifying a project branch.

### Task A2: Create and verify lesson tag

- Create annotated tag `lesson/07` on VN merge commit `3f834a4e`.
- Push only `refs/tags/lesson/07` to GitHub.
- Verify the remote tag resolves to `3f834a4e` using `git rev-list -n1`/`git ls-remote`.
- Run `pnpm lesson 07` or the repository-supported lesson verification from a clean worktree.

## Phase B — GitLab English mirror

### Task B1: Audit GitLab baseline

- Fetch `gitlab` explicitly and inspect `gitlab/example/nestjs-training` tip, author, and tree.
- Compare the GitHub EN tree `origin/example/nestjs-training` with the GitLab baseline.
- Never use a broad `git push --all` or `--mirror`.

### Task B2: Build a clean same-tree GitLab commit

- Use an isolated worktree based on the fresh GitLab branch.
- Replace its content with the verified GitHub EN tree.
- Preserve English-only content, file names, and comments.
- Create one clean mirror commit with author `hienduong-agility` and no `Co-authored-by` trailer.
- Push explicitly to `gitlab HEAD:example/nestjs-training` using the approved remote.

### Task B3: Verify GitLab result

- Read back the GitLab remote tip, author, commit message, and tree.
- Compare GitHub EN and GitLab EN content trees; expected diff is empty.
- Scan the full GitLab tree, including extensionless files, for Vietnamese diacritics and common undiacriticized Vietnamese words.
- Record runtime status as `SKIPPED / UNVERIFIED` for PostgreSQL; do not claim live DB evidence.

## Phase C — Linear L08 resolution

### Task C1: Read the authoritative L08 ticket set

- Dispatch a read-only Linear reader through the approved herdr pane/profile.
- Resolve the L08 parent and all children, including implementation, theory, review, and quiz tasks.
- Capture title, status, labels, assignee, Linear-generated branch, acceptance criteria, dependencies, and checklist/DoD.
- Write a nonce-bearing local sentinel and validate it against the live issue data.
- If Linear MCP remains unavailable, stop and report the blocker rather than guessing L08 scope.

### Task C2: Map ownership and scope

- Separate reference implementation, lesson note/theory, counter-view, review, and learner-owned hands-on/quiz work according to the ticket labels and repository policy.
- Preserve the one-time execution-substitute scope; do not silently apply it to L08 unless explicitly authorized.
- Confirm exact branch/worktree names from Linear before creating worktrees.

## Phase D — L08 dispatch (only after C1/C2)

### Task D1: Prepare isolated worktrees

- Create fresh worktrees from `origin/main` using the exact Linear branch names.
- Verify each worktree path, branch, `AGENTS.md`, `CLAUDE.md`, and relevant L08 `SPEC.md` before dispatch.
- Do not edit VN `main` or the existing lesson worktrees.

### Task D2: Dispatch approved agents

- Report agent, model/runtime, task scope, branch, worktree, forbidden actions, sentinel path, and verification plan before dispatch.
- Use Claude for routed lesson/docs/mentor work, Codex for reference implementation when the ticket permits it, and Agy for an independent counter-view or translation review.
- Do not dispatch until the user approves the exact L08 mutation/ownership scope if the ticket introduces learner-owned hands-on or quiz work.

### Task D3: Verify and open PRs

- Require sentinel plus independent verification: raw diff, `pnpm verify`, focused tests, build, and runtime evidence.
- Keep live PostgreSQL verification separate from static verification; if Docker remains waived, disclose the exact unverified gates.
- Run review gates, create separate PRs with accurate Linear linkage, and stop at the user merge gate.

## Files / remotes likely involved

- `docs/ROADMAP.md`
- `docs/lessons/07-prisma-postgresql/README.md`
- `docs/lessons/_agent-log.md`
- `origin/main`
- `origin/example/nestjs-training`
- `gitlab/example/nestjs-training`
- `.hermes/runs/*.json` (local ignored evidence only)

## Validation checklist

- [ ] PR #90 verified merged.
- [ ] `lesson/07` exists remotely and points to VN merge commit `3f834a4e`.
- [ ] GitHub EN and GitLab EN trees are identical.
- [ ] GitLab tip author is `hienduong-agility`; no trailer is present.
- [ ] Full EN scan is clean.
- [ ] Linear L08 parent/children are read from the live source of truth.
- [ ] L08 branch names and acceptance criteria are not guessed.
- [ ] L08 worktrees are isolated and clean before dispatch.
- [ ] PostgreSQL runtime remains explicitly marked unverified unless real DB evidence exists.
- [ ] No PR is merged by Hermes.

## Risks / blockers

- GitLab authentication or non-fast-forward state may require a fresh remote baseline and `--force-with-lease`; never use blind force.
- Linear MCP may remain unavailable; in that case L08 cannot be responsibly scoped from roadmap text alone.
- GitHub CI may not run on the EN branch; use local verification and report absent checks honestly.
- A stale remote-tracking ref may make `git fetch --prune` partially fail; repair only the local ref after reading its current state.
