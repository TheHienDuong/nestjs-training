# L06 Lesson Start — Configuration & Environment Variables

## Goal

Start L06 through the repository workflow after L05 closeout. This task covers source-of-truth resolution and lesson-start artifacts only; it does not implement the hands-on code.

## Steps

1. Verify origin/main, open PRs, tags, GitLab parity, and repo cleanliness.
2. Resolve the exact Linear parent/children for L06 via Claude + Linear MCP; do not guess identifiers or branch names.
3. Create an isolated lesson-start branch from origin/main and dispatch Claude mentor/PM through herdr.
4. Claude reads workflow/SPEC conventions and official Nest configuration docs, creates the VN README/SPEC and log entry, and moves only the L06 parent to In Progress. Children remain Backlog.
5. Hermes independently reads sentinel/raw diff, runs docs verification, confirms src/test/package untouched, and reports the next user action.

## Boundaries

- No implementation in src/ or test/.
- No dependency installation.
- No quiz/hands-on completion.
- No ROADMAP ✅; at most lesson-start state according to existing workflow.
- No PR/push unless the lesson-start workflow explicitly requires it.
- Linear mutation only through Claude single-writer and only for parent start state.
